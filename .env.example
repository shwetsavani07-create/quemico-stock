# Option 1: full connection string (URL-encode special chars in password, e.g. @ becomes %40)
MONGODB_URI=

# Option 2: build URI from parts (password is encoded automatically)
# MONGODB_USERNAME=
# MONGODB_PASSWORD=
# MONGODB_HOST=your-cluster.xxxxx.mongodb.net
# MONGODB_DB=quemico-stock
